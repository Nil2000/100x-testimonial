# GitHub Actions secrets for .github/workflows/deploy_processor.yml
# (written to /opt/testiflow/.env on each manual deploy)

REDIS_PASSWORD=
REDIS_QUEUE=text-queue
APP_URL=
INTERNAL_API_KEY=
OPENROUTER_API_KEY=
